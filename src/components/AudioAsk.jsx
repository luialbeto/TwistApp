import React, { useState } from 'react';

function AudioAsk() {
  const [voice, setVoice] = useState();

  return (
    <div>
      <button src={} onClick={() => ()}>
        <img alt="" width="" />
      </button>
    </div>
  );
}

export default AudioAsk;
