import React, { useState } from 'react';

function AudioAsk() {
  const [audio, setAud] = useState();

  return (
    <div>
      <button src={} onClick={() => ()}>
        <img alt="" width="" />
      </button>
    </div>
  );
}

export default AudAsk;
